div
   div(ui-ace="{onLoad: vm.aceLoaded, mode: 'c_cpp'}")
   div
      span Language du fichier :
      select(ng-model="vm.language", ng-options="option.id as option.label for option in vm.languageOptions track by option.id", ng-change="vm.setLanguage(option)")
