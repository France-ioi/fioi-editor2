div
   div(ui-ace="{onLoad: vm.aceLoaded, mode: 'c_cpp'}")
   div
      span Language du fichier :
      select(ng-model="vm.language", ng-options="option.name for option in vm.languageOptions track by option.name", ng-change="vm.setLanguage(option)")
